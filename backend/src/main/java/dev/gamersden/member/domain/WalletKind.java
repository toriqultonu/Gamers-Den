package dev.gamersden.member.domain;

/** {@code wallet_ledger.kind} (the CHECK swapped in after the LIKE-clone). */
public enum WalletKind {
    TOPUP,
    SPEND,
    POINTS_CONVERSION,
    REVERSAL
}
