import { ExpensesScreen } from '@/components/domain/expenses-screen';

/**
 * S8 — Expenses (TASK F12). Every operator records petty cash against the shift
 * that is open on their terminal; no guard, for the same reason as S7.
 */
export default function ExpensesPage() {
  return <ExpensesScreen />;
}
